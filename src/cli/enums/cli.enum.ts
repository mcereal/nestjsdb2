export enum Commands {
  INSTALL = "install",
  UNINSTALL = "uninstall",
  CHECK = "check",
  UPDATE = "update",
  CONFIG = "config",
  LIST_INSTALLED_DRIVERS = "list",
  LIST_REMOTE_DRIVERS = "--list-remote",
}
