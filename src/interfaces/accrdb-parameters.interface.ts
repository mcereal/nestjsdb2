// src/interfaces/accrdb-parameters.interface.ts

// Interface representing AccrDB parameters.
export interface AccrDBParameters {
  [key: string]: any; // Replace 'any' with specific parameter types where possible
}
